{
  "name": "desktopsingux",
  "version": "1.0.0",
  "description": "",
  "main": "test.js",
  "scripts": {
    "i": "npm install && npx @neutralinojs/neu update && powershell -Command \"Remove-Item -Path public/assets/scripts/*.ts -Force\"",
    "start": "concurrently  \"npx @neutralinojs/neu run\"  \"node back/server.js\"",
    "front": "npx @neutralinojs/neu run",
    "back": "node back/server.js",
    "build": "echo todo :)"
  },
  "keywords": [],
  "author": "Louis-Charles Biron",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.1.0",
    "ws": "^8.18.0"
  }
}
